export default Object.freeze({
  select_out:
    'relative  flex flex-1 cursor-pointer items-center justify-between rounded-lg border border-stroke-primary p-2 ',
  modal_out:
    'bg-black_gradient top-0 fixed bottom-0 left-0 right-0  z-20 backdrop-blur-sm transition duration-300 ease-in',
  modal_main: 'flex flex-col bg-background-2 relative p-5 bg-blend-luminosity',
});
